# GymAssistant
- pip install -r requirements.txt per installare le dipendenze
- Prima di avviare il progetto, lanciare il comando python src/rag/init_rag.py per inizializzare il rag
- Lanciare il file src/model_loader.py per caricare il modello finetunato
- Per avviare il progetto eseguire il comando langgraph dev
